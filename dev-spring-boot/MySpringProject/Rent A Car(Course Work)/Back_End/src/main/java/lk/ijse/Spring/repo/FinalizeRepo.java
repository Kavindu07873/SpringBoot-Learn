package lk.ijse.Spring.repo;

import lk.ijse.Spring.entity.Finalize;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FinalizeRepo extends JpaRepository<Finalize,String> {
}
